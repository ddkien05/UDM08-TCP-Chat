<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- ================= COLORS ================= -->

    <SolidColorBrush x:Key="PrimaryBrush"
                     Color="#2563EB"/>

    <SolidColorBrush x:Key="SecondaryBubbleBrush"
                     Color="#F3F4F6"/>

    <SolidColorBrush x:Key="BackgroundBrush"
                     Color="#FFFFFF"/>

    <!-- BORDER -->

    <SolidColorBrush x:Key="BorderColor"
                     Color="#E5E7EB"/>

    <!-- TEXT -->

    <SolidColorBrush x:Key="TextColor_Primary"
                     Color="#111827"/>

    <SolidColorBrush x:Key="TextColor_Secondary"
                     Color="#6B7280"/>

    <!-- ================= STATUS ================= -->

    <!-- Online = xanh -->
    <SolidColorBrush x:Key="OnlineStatus"
                     Color="#22C55E"/>

    <!-- Offline = đỏ -->
    <SolidColorBrush x:Key="OfflineStatus"
                     Color="#EF4444"/>

    <!-- ================= TYPOGRAPHY ================= -->

    <FontFamily x:Key="AppFontFamily">
        Assets/Fonts/#Roboto
    </FontFamily>

    <!-- HEADER -->

    <Style x:Key="HeaderMedium"
           TargetType="TextBlock">

        <Setter Property="FontFamily"
                Value="{StaticResource AppFontFamily}"/>

        <Setter Property="FontSize"
                Value="16"/>

        <Setter Property="FontWeight"
                Value="Medium"/>

        <Setter Property="Foreground"
                Value="{StaticResource TextColor_Primary}"/>

    </Style>

    <!-- BODY -->

    <Style x:Key="BodyRegular"
           TargetType="TextBlock">

        <Setter Property="FontFamily"
                Value="{StaticResource AppFontFamily}"/>

        <Setter Property="FontSize"
                Value="14"/>

        <Setter Property="FontWeight"
                Value="Normal"/>

        <Setter Property="Foreground"
                Value="{StaticResource TextColor_Primary}"/>

    </Style>

    <!-- CAPTION -->

    <Style x:Key="Caption"
           TargetType="TextBlock">

        <Setter Property="FontFamily"
                Value="{StaticResource AppFontFamily}"/>

        <Setter Property="FontSize"
                Value="12"/>

        <Setter Property="FontWeight"
                Value="Normal"/>

        <Setter Property="Foreground"
                Value="{StaticResource TextColor_Secondary}"/>

    </Style>

</ResourceDictionary>