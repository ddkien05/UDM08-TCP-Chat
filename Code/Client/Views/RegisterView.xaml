<UserControl x:Class="ChatTCP.Client.Views.RegisterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="400"
             Background="{StaticResource BackgroundBrush}">

    <Grid>
        <Border Width="420" Padding="24" HorizontalAlignment="Center" VerticalAlignment="Center" CornerRadius="8" BorderBrush="{StaticResource BorderColor}" BorderThickness="1" Background="{StaticResource BackgroundBrush}">
            <StackPanel>
                <TextBlock Text="Đăng Ký" Style="{StaticResource HeaderMedium}" Margin="0,0,0,12" HorizontalAlignment="Center"/>

                <TextBlock Text="Username" Style="{StaticResource Caption}"/>
                <TextBox x:Name="UsernameTextBox" Height="36" Margin="0,4,0,12"/>

                <TextBlock Text="Display name" Style="{StaticResource Caption}"/>
                <TextBox x:Name="DisplayNameTextBox" Height="36" Margin="0,4,0,12"/>

                <TextBlock Text="Password" Style="{StaticResource Caption}"/>
                <PasswordBox x:Name="PasswordBox" Height="36" Margin="0,4,0,12"/>

                <TextBlock x:Name="ErrorText" Foreground="Red" Visibility="Collapsed" TextWrapping="Wrap" Margin="0,0,0,8"/>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- Back -->
                    <Button x:Name="BackButton"
            Grid.Column="2"
            Content="Quay về"
            Width="100"
            Click="BackButton_Click"/>

                    <!-- Register -->
                    <Button x:Name="RegisterButton"
            Grid.Column="0"
            Content="Đăng ký"
            Width="100"
            Style="{StaticResource ChatSendButton}"
            Click="RegisterButton_Click"/>
                </Grid>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
