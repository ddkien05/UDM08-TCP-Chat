<UserControl x:Class="ChatTCP.Client.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Background="{StaticResource BackgroundBrush}">

        <Border Width="360"
                Padding="30"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                BorderBrush="{StaticResource BorderColor}"
                BorderThickness="1"
                CornerRadius="12">

            <StackPanel>

                <TextBlock Text="Chat"
                           FontSize="28"
                           FontWeight="Bold"
                           HorizontalAlignment="Center"
                           Margin="0,0,0,30"/>

                <TextBlock Text="Tên đăng nhập"
                           Margin="0,0,0,6"/>

                <TextBox x:Name="UsernameTextBox"
                         Height="40"
                         Padding="10"
                         Margin="0,0,0,16"/>

                <TextBlock Text="Mật khẩu"
                           Margin="0,0,0,6"/>

                <PasswordBox x:Name="PasswordBox"
                             Height="40"
                             Padding="10"
                             Margin="0,0,0,20"/>

                <TextBlock x:Name="ErrorText"
                           Foreground="Red"
                           Visibility="Collapsed"
                           TextWrapping="Wrap"
                           Margin="0,0,0,10"/>

                <Button Content="Đăng nhập"
                        Height="42"
                        Click="LoginButton_Click"/>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,12,0,0">
                    <TextBlock Text="Don't have an account?" Margin="0,0,8,0" VerticalAlignment="Center"/>
                    <Button x:Name="RegisterNavButton" Content="Register" Click="RegisterNavButton_Click"/>
                </StackPanel>

            </StackPanel>

        </Border>

    </Grid>

</UserControl>